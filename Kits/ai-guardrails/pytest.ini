[pytest]
markers =
    integration: makes real Groq API calls through the full hardened pipeline; requires a configured .env and network access. Excluded by default -- run with: pytest -m integration
testpaths = tests
addopts = -m "not integration"
